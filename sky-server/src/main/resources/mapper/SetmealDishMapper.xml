<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <insert id="insertBatch">
        insert into sky_take_out.setmeal_dish (setmeal_id, dish_id, name, price, copies) VALUE
            <foreach collection="list" separator="," item="sd">
                (#{sd.setmealId}, #{sd.dishId}, #{sd.name}, #{sd.price}, #{sd.copies})
            </foreach>
    </insert>

    <delete id="deleteBysetMealId">
        delete from sky_take_out.setmeal_dish where setmeal_id in
        <foreach collection="list" item="setmealId" separator="," open="(" close=")">
            #{setmealId}
        </foreach>
    </delete>



    <select id="getsetmealIdsByDishIds" resultType="java.lang.Long">
        select setmeal_id from sky_take_out.setmeal_dish where dish_id in
            <foreach collection="list" item="dishId" open="(" close=")" separator=",">
                #{dishId}
            </foreach>
    </select>
</mapper>
