<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">
    <insert id="insertBatch">
        insert into sky_take_out.order_detail (name, image, order_id, dish_id, setmeal_id, dish_flavor, number, amount) VALUES
        <foreach collection="list" item="od" separator=",">
            (#{od.name}, #{od.image}, #{od.orderId}, #{od.dishId}, #{od.setmealId}, #{od.dishFlavor}, #{od.number}, #{od.amount} )
        </foreach>
    </insert>
    <select id="top10" resultType="com.sky.entity.Sales">
        SELECT od.name as name, sum(od.number) as number
        from order_detail od
        left JOIN orders o ON od.order_id = o.Id
        <where>
            <if test="begin != null">and o.order_time >= #{begin}</if>
            <if test="end != null">and o.order_time &lt; #{end}</if>
            <if test="status != null">and o.status = #{status}</if>
        </where>
        GROUP BY od.dish_id, od.`name`, od.setmeal_id
        order by number DESC
        LIMIT 0, 10
    </select>
</mapper>